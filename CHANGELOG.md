# 변경 이력

## Unreleased

### 모집단위 명칭 검색 개선

- 사용자가 입력한 학과·학부·전공명과 대학 모집요강의 실제 모집단위 명칭이 다른 경우를 처리하도록 검색어 확장 기능을 추가했습니다.
- 대학별 특수 관계를 `config/major_aliases.json`에서 관리하도록 구성했습니다.
- 연세대학교의 `컴퓨터과학과` 질문은 실제 모집단위인 `첨단컴퓨팅학부`를 함께 검색하고, 통합 모집 후 2학년 진급 시 세부전공을 선택하는 관계를 답변에 표시합니다.
- 컴퓨터 계열의 일반적인 명칭 차이는 선택된 대학의 모집요강에 실제 명칭이 존재할 때만 자동으로 확장합니다.
  - 컴퓨터과학과·컴퓨터과학부
  - 컴퓨터공학과·컴퓨터공학부
  - 컴퓨터학과
  - 컴퓨터소프트웨어학부
  - 소프트웨어학과·지능형소프트웨어학과
- 대학별 특수 관계 설정을 우선하고, 설정이 없을 때만 문서 기반 자동 탐색을 사용하도록 했습니다.

### 검색 정확도 개선

- 실제 모집단위 별칭이 적용된 모집인원 질문에서는 별칭이 직접 등장하고 숫자 근거가 풍부한 페이지를 우선하도록 변경했습니다.
- 별칭 모집단위가 포함된 페이지를 답변 컨텍스트에 우선 전달하도록 정렬 기준을 추가했습니다.
- 모집인원과 경쟁률·배점·예비번호 등 다른 숫자를 혼동하지 않도록 모집인원 전용 프롬프트를 보강했습니다.
- 직접 모집과 통합 모집 후 전공 선택을 동일한 모집단위로 잘못 설명하지 않도록 했습니다.
- 확인된 모집단위 관계가 LLM 답변에서 누락되면 최종 답변에 관계 설명을 추가하도록 했습니다.

### 존재하지 않는 모집단위 처리

- NER 결과뿐 아니라 사용자 질문 원문에서도 `학과`, `학부`, `전공` 형태의 모집단위명을 추출합니다.
- 요청한 모집단위가 해당 대학 모집요강 전체에 존재하는지 확인합니다.
- 요청 명칭과 검증된 대체 명칭을 모두 찾을 수 없는 경우 LLM을 호출하지 않고 다음 형식의 안내를 반환합니다.

  ```text
  지금 검색한 단어인 'OO학과'는 OO대학교 모집요강에서 찾을 수 없습니다. 해당 대학에서 사용하는 다른 학과·학부 또는 모집단위 명칭으로 검색해 주세요.
  ```

- 존재하지 않는 모집단위 안내에는 관련 없는 PDF 위치와 페이지를 근거로 반환하지 않습니다.
- 검색 실패와 실제로 해당 사항이 없는 경우를 구분하도록 했습니다.

### 테스트 및 문서

- 대학별 별칭 확장, 문서 기반 모집단위 자동 탐색, 정확한 명칭 우선 처리 테스트를 추가했습니다.
- NER이 모집단위명을 놓친 경우 질문 원문에서 복구하는 테스트를 추가했습니다.
- 존재하지 않는 모집단위의 고정 안내문 테스트를 추가했습니다.
- README에 모집단위 별칭 관리 및 자동 탐색 방식을 문서화했습니다.

### 운영 시 유의사항

- Flask 서버가 `use_reloader=False`로 실행되므로 코드 변경 후 서버를 재시작해야 합니다.
- `__pycache__`와 `.pyc`는 실행 시 생성되는 캐시 파일이므로 커밋 대상에서 제외합니다.
- 현재 로컬 Python 실행 경로 문제로 전체 pytest 통합 실행은 수행하지 못했으며, Git diff 정적 검사는 통과했습니다.
